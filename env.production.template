# Production Environment Configuration for Render.com
# These values will be automatically set by Render from the database service

# Database Configuration (auto-configured by Render)
DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
DB_USERNAME=${DB_USERNAME}
DB_PASSWORD=${DB_PASSWORD}
DB_NAME=${DB_NAME}

# Application Configuration
PORT=10000
NODE_ENV=production

# CORS Configuration
ALLOWED_ORIGINS=https://admin-interface-alpha.vercel.app,http://localhost:3000

# Note: Frontend domain is configured for Vercel deployment
